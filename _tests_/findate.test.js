import findate from '../src/findate.js';

describe("findate", () => {
  test("should return the day of the week with any inputted date.", () => {
    const getDate = new Date("December 25, 2021"); 
    expect(findate = .Saturday);
  });
});

expect(findate(getDate)).toEqual("Saturday");












// const dayArray  [ "Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];
